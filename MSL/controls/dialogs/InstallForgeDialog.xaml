<UserControl x:Class="MSL.controls.InstallForgeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:MSL.controls"
        xmlns:dialog="clr-namespace:MSL.controls.dialogs"
        xmlns:langs="clr-namespace:MSL.langs"
        Height="400" Width="750"
        mc:Ignorable="d" Template="{StaticResource UserControlTemplate}" Loaded="UserControl_Loaded" >
    <Border CornerRadius="10" Background="{DynamicResource SecondaryRegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" Text="{Binding [SR_NeoForgeInstaller], Source={x:Static langs:LanguageManager.Instance}}" FontSize="20" x:Name="InstallDialogTitle"/>
            <ComboBox x:Name="Mirror" Grid.Row="0" HorizontalAlignment="Left" Margin="10" SelectionChanged="Mirror_SelectionChanged">
                <ComboBoxItem Content="{Binding [SR_SourceMSL], Source={x:Static langs:LanguageManager.Instance}}" IsSelected="True"/>
                <ComboBoxItem Content="{Binding [SR_SourceMSLBackup], Source={x:Static langs:LanguageManager.Instance}}"/>
                <ComboBoxItem Content="{Binding [SR_SourceOfficial], Source={x:Static langs:LanguageManager.Instance}}"/>
            </ComboBox>
            <ComboBox x:Name="MultiThreadCount" Grid.Row="0" HorizontalAlignment="Right" Margin="10" SelectionChanged="MultiThreadCount_SelectionChanged">
                <ComboBoxItem Content="{Binding [SR_Concurrency1], Source={x:Static langs:LanguageManager.Instance}}"/>
                <ComboBoxItem Content="{Binding [SR_Concurrency2], Source={x:Static langs:LanguageManager.Instance}}"/>
                <ComboBoxItem Content="{Binding [SR_Concurrency3], Source={x:Static langs:LanguageManager.Instance}}"/>
                <ComboBoxItem Content="{Binding [SR_Concurrency4], Source={x:Static langs:LanguageManager.Instance}}" IsSelected="True"/>
                <ComboBoxItem Content="{Binding [SR_Concurrency5], Source={x:Static langs:LanguageManager.Instance}}"/>
                <ComboBoxItem Content="{Binding [SR_Concurrency6], Source={x:Static langs:LanguageManager.Instance}}"/>
                <ComboBoxItem Content="{Binding [SR_Concurrency7], Source={x:Static langs:LanguageManager.Instance}}"/>
                <ComboBoxItem Content="{Binding [SR_Concurrency8], Source={x:Static langs:LanguageManager.Instance}}"/>
            </ComboBox>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="log" Margin="10,0,10,0" Padding="7" TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top"/>
                <dialog:DownloadDisplayControl Grid.Column="1" x:Name="DownloadDisplay" Margin="0,0,10,0" />
            </Grid>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" >
                <TextBlock Foreground="{DynamicResource PrimaryTextBrush}" Margin="10" VerticalAlignment="Center" TextWrapping="Wrap" Text="{Binding [SR_CurrentStatus], Source={x:Static langs:LanguageManager.Instance}}" FontSize="16"/>
                <TextBlock x:Name="status" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" TextWrapping="Wrap" Text="{Binding [SR_WaitingInstall], Source={x:Static langs:LanguageManager.Instance}}" FontSize="16"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button x:Name="ChangePlanButton" Content="{Binding [SR_UseCmdInstall], Source={x:Static langs:LanguageManager.Instance}}" Click="ChangePlanButton_Click" Margin="10,10,0,10"/>
                <Button x:Name="CancelButton" Style="{StaticResource ButtonPrimary}" Content="{Binding [Cancel], Source={x:Static langs:LanguageManager.Instance}}" Click="CancelButton_Click" Margin="10"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
