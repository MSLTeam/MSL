<hc:Window x:Class="MSL.SetFrpc"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:controls="clr-namespace:MSL.controls"
        xmlns:local="clr-namespace:MSL"
        mc:Ignorable="d"
        NonClientAreaBackground="{DynamicResource SideMenuBrush}" Background="{DynamicResource BackgroundBrush}"
        Title="MSL-配置内网映射" Height="450" Width="750" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid x:Name="MainGrid">
        <TabControl Background="{x:Null}">
            <TabItem Header="MSL-FRP" Background="{DynamicResource SideMenuBrush}">
                <Frame Source="/pages/frpProviders/MSLFrp.xaml"/>
            </TabItem>
            <TabItem Header="OpenFRP" Background="{DynamicResource SideMenuBrush}">
                <Frame Source="/pages/frpProviders/OpenFrp.xaml"/>
            </TabItem>
        </TabControl>
    </Grid>
</hc:Window>