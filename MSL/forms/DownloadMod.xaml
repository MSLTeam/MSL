<hc:Window x:Class="MSL.DownloadMod"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:controls="clr-namespace:MSL.controls"
        xmlns:local="clr-namespace:MSL"
        mc:Ignorable="d"
        NonClientAreaBackground="{DynamicResource BackgroundBrush}"
        Title="下载Mod" Height="450" Width="750" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Loaded="Window_Loaded" Closing="Window_Closing" Background="{DynamicResource BackgroundBrush}">
    <Grid x:Name="BodyGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <hc:UniformSpacingPanel Grid.Row="0" Spacing="10" Margin="10">
            <Button x:Name="backBtn" Content="返回" Click="backBtn_Click" Width="120" IsEnabled="False" FontSize="14"/>
            <StackPanel Orientation="Horizontal">
                <Label Content="搜索Mod：" FontSize="16" BorderBrush="{x:Null}" Background="{x:Null}" Padding="10,0,10,0"/>
                <TextBox x:Name="textBox1" MinWidth="150"/>
                <Button x:Name="searchMod" Content="搜索" Click="searchMod_Click" Width="100" FontSize="14" IsEnabled="False"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="下载源：" FontSize="16" BorderBrush="{x:Null}" Background="{x:Null}" Padding="10,0,10,0"/>
                <ComboBox>
                    <ComboBoxItem Content="CurseForge" IsSelected="True"/>
                    <ComboBoxItem Content="Modrinth（开发中）" IsEnabled="False"/>
                </ComboBox>
            </StackPanel>
        </hc:UniformSpacingPanel>
        <ListView Grid.Row="1" x:Name="listBox" MouseDoubleClick="listBox_MouseDoubleClick" Background="{DynamicResource BorderBackground}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn x:Name="listBoxColumnName" Width="500">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Image Height="18" Width="18" Name="imageName"  Source="{Binding Icon}" HorizontalAlignment="Left"/>
                                    <TextBlock Margin="30,0,0,0" Width="Auto" Text="{Binding State}"/>
                                </Grid>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <hc:LoadingCircle Grid.Row="1" x:Name="lCircle" HorizontalAlignment="Center" VerticalAlignment="Center" Width="50" Margin="0,0,150,0"/>
        <Label Grid.Row="1" x:Name="lb01" Content="加载中，请耐心等待……" Margin="50,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{DynamicResource DarkPrimaryBrush}"/>
    </Grid>
</hc:Window>
