<Window x:Class="MSL.DownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MSL"
        mc:Ignorable="d"
        Title="MSL-Downloading..." Height="103.238" Width="397.684" Loaded="Window_Loaded" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Background="{x:Null}" WindowStyle="None" AllowsTransparency="True">
    <Border x:Name="mainBorder" CornerRadius="5" Margin="5,4,5,5" MouseLeftButtonDown="mainBorder_MouseLeftButtonDown">
        <Border.Background>
            <SolidColorBrush Color="#FFF1F3F8"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <TextBlock x:Name="infolabel" HorizontalAlignment="Left" Margin="10,17,0,0" TextWrapping="Wrap" Text="加载中……" VerticalAlignment="Top"/>
            <ProgressBar x:Name="pbar" HorizontalAlignment="Center" Height="11" Margin="0,37,0,0" VerticalAlignment="Top" Width="368" Background="#E5C8C8C8"/>
            <Button x:Name="button1" Content="取消" HorizontalAlignment="Left" Margin="303,53,0,0" VerticalAlignment="Top" Width="75" Click="button1_Click" FontFamily="Microsoft YaHei UI" FontSize="14" FontWeight="Normal" MouseDoubleClick="button1_MouseDoubleClick"/>
            <Label x:Name="taskinfo" Content="当前任务：" HorizontalAlignment="Left" Margin="10,52,0,0" VerticalAlignment="Top"/>
        </Grid>
    </Border>
</Window>
